services:
  jota-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jota_gateway
    network_mode: "host"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./data:/app/data
